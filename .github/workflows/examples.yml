name: Examples

on: [push]
jobs:
  build:
    name: Greeting
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: |
          wget https://void.altervista.org/manifest.json
          echo "done"